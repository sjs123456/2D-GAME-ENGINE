# 嵌入式编程学习路线图（从零开始）

> 面向：会一点编程（学过 C/Python）、没碰过硬件的学习者
> 路径：**单片机打底 → RTOS → Linux 嵌入式**，全程约 12~18 个月
> 原则：**动手 > 看视频**，每阶段必须有可演示的产出；学不会就重做，不要背答案

---

## 路线总览

```
┌─ 阶段0 准备（1周）────────────────────────────┐
│  买板子 + 搭环境，C 语言补强                   │
└───────────────────────────────────────────────┘
        │
┌─ 阶段1 C语言补强（2~4周）─────────────────────┐
│  指针/结构体/位运算/内存 —— 嵌入式版C         │
└───────────────────────────────────────────────┘
        │
┌─ 阶段2 电路基础（2~3周，并行）────────────────┐
│  欧姆定律/上下拉/原理图/数字电路基础          │
└───────────────────────────────────────────────┘
        │
┌─ 阶段3 51单片机打底（2~4周，可选但推荐）──────┐
│  GPIO/定时器/中断/串口，理解寄存器本质        │
└───────────────────────────────────────────────┘
        │
┌─ 阶段4 STM32裸机（核心，2~3个月）─────────────┐
│  HAL库+ CubeMX → 外设逐个击破 → 综合项目      │
└───────────────────────────────────────────────┘
        │
┌─ 阶段5 FreeRTOS（1个月）──────────────────────┐
│  任务/信号量/队列 → 多任务项目                │
└───────────────────────────────────────────────┘
        │
├─ 阶段6 硬件进阶（并行，立创EDA画板+焊接）─────┤
│                                                │
└──────────┬─────────────────────────────────────┘
           │ 以下为 Linux 嵌入式分支（选学/进阶）
┌─ 阶段7 Linux系统+应用编程（2~3个月）──────────┐
│  Linux使用 → 文件IO/进程/线程/网络 → 交叉编译 │
└───────────────────────────────────────────────┘
        │
┌─ 阶段8 嵌入式Linux系统+驱动（2~3个月）────────┐
│  uboot/内核/根文件系统 → 字符设备驱动/设备树  │
└───────────────────────────────────────────────┘
        │
┌─ 阶段9 综合项目 + 求职准备（2~3个月）─────────┐
│  物联网网关/智能家居项目 → 简历+八股          │
└───────────────────────────────────────────────┘
```

---

## 阶段 0：准备（约 1 周）

### 硬件采购清单（按优先级）

| 物品 | 推荐 | 预算 |
|------|------|------|
| STM32 开发板 | 正点原子/野火 STM32F103 精英版（资料最全） | ¥150~250 |
| 51 开发板 | 普中/江协科技（二手的也行） | ¥50~100 |
| USB 转串口模块 | CH340，调试必备 | ¥10 |
| 杜邦线/面包板/万用表 | 万用表买 30 元以上的数字表 | ¥50 |
| 逻辑分析仪 | 先不买，用到再买（¥30 的 8 通道即可） | 后期 |
| 示波器 | 二手 100MHz 或台式机；初期可不买 | 后期 |

> 别囤板子：**一块 STM32 + 一块 51 足够走完单片机阶段**。外设模块（OLED、DHT11、MPU6050、超声波、蓝牙等）按项目需要再买，单件 5~20 元。

### 软件环境

- **Keil MDK**（51 和 STM32 通用）或 **STM32CubeIDE**（免费，Linux 也能用）
- **STM32CubeMX**：图形化配置引脚/时钟/外设，生成初始化代码
- **串口调试助手**（SSCOM 或 XCOM）
- **立创EDA**：画原理图和 PCB（免费，后面画板用）

### 推荐资源

- B站：**江协科技**（51/STM32 视频课，口碑最好）、**正点原子**、**野火**
- 书籍：《STM32 嵌入式系统开发实战指南》或正点原子《STM32F103 开发指南》
- 社区：正点原子论坛、CSDN、电子发烧友

---

## 阶段 1：C 语言补强（2~4 周）

嵌入式 C 和考试 C 不一样，重点补：

- **指针**：指针与数组、函数指针、多级指针（驱动回调全靠它）
- **结构体/联合体**：寄存器组定义、位域操作
- **位运算**：`& | ^ ~ << >>`，置位/清位/翻转的惯用法
- **内存**：栈/堆/全局区、`static`/`volatile`/`const` 的嵌入式语义
- **预处理**：宏定义、条件编译（HAL 库大量使用）
- **编译链接**：.c → .o → .elf → .hex 的过程，理解"编译不过 = 缺头文件/链接不过 = 缺实现"

**产出**：用 C 写一个小工具，比如链表、环形缓冲区（后面串口接收会用）。

资源：翁恺《C语言程序设计》（B站）、《C Primer Plus》当字典翻。

> 会 Python 的话，理解"变量是内存的别名"比 Python 的"名字绑定"更接近硬件真相。

---

## 阶段 2：电路基础（2~3 周，与阶段 3 并行）

不用学成电子工程师，够用就行：

- **基础**：欧姆定律、串并联、电压/电流/功率
- **数字电路**：与或非门、触发器、上拉/下拉电阻、开漏输出
- **读原理图**：电源、晶振、复位、下载电路，能看懂开发板原理图
- **示波器/万用表**：量电压、测通断、看波形
- 焊接：买练习板练直插/贴片焊接（后面画板要用）

**产出**：能独立把开发板原理图讲明白（电源怎么来、LED 为什么这样接）。

---

## 阶段 3：51 单片机打底（2~4 周，可选但强烈推荐）

51 的目的是**用最少的封装理解"操作寄存器 = 控制硬件"**。

必做实验（跟着江协科技或普中教程）：

1. 点亮 LED（GPIO 输出）
2. 按键控制（GPIO 输入 + 消抖）
3. 数码管动态扫描（定时器）
4. 定时器中断（流水灯、精确延时）
5. 串口通信（发送/接收，接电脑收发数据）
6. 中断优先级 + 外部中断

**产出**：一个综合小项目，如"电子时钟（数码管+按键+定时器）"或"智能温控风扇"。

> 有人建议跳过 51 直接 STM32，可行但 51 只要 2~3 周，能把"寄存器思维"焊进脑子，后面学 HAL 库时你会感谢它。

---

## 阶段 4：STM32 裸机（核心阶段，2~3 个月）

这是最重要的一站，决定你能不能找到单片机岗。

### 4.1 工具链（1 周）

- 用 **STM32CubeMX 生成工程 + HAL 库**，理解时钟树（SYSCLK/APB1/APB2）、引脚复用
- 学会查**数据手册**和**参考手册**（寄存器描述、引脚定义表）

### 4.2 外设逐个击破（6~8 周，每学一个做一个小实验）

| 外设 | 实验 | 考点 |
|------|------|------|
| GPIO | 按键中断 + LED 呼吸灯 | 开漏/推挽、上下拉 |
| 定时器 | PWM 控制电机/呼吸灯、输入捕获测频率 | 预分频/重装值计算 |
| USART | 串口收发 + 中断接收 + 环形缓冲 | 波特率、中断处理 |
| I2C | 读 OLED（SSD1306）/MPU6050 | 时序、从机地址 |
| SPI | 读 Flash（W25Q64）或 LCD | 时钟极性相位 CPOL/CPHA |
| ADC/DAC | 采集电位器电压、DHT11 温湿度 | 分辨率、参考电压 |
| DMA | 串口/ADC 用 DMA 传输 | 中断 vs DMA 区别 |
| 看门狗/低功耗 | 喂狗实验、睡眠唤醒 | 面试常问 |

### 4.3 综合项目（2~3 周，任选 1~2 个）

- **智能小车**：PWM 驱动 + 超声波避障 + 蓝牙遥控（最经典）
- **环境监测站**：DHT11 + OLED + 串口上报 + 按键设置阈值
- **智能门锁/宿舍打卡机**：矩阵键盘 + OLED + 存储

**产出**：GitHub 上 2~3 个完整项目（含原理图/README/演示视频），这是简历的核心。

---

## 阶段 5：FreeRTOS（约 1 个月）

裸机能写项目后，上 RTOS 理解"并发"：

- 任务/调度（抢占式 vs 时间片）、任务状态机
- 信号量（二值/计数）、互斥锁（优先级翻转）、队列、软件定时器
- 内存管理（heap_1~heap_4）、中断与临界区
- 在 STM32 上移植 FreeRTOS，用 CubeMX 直接生成

**项目**：把阶段 4 的环境监测站改成多任务版（采集任务 + 显示任务 + 通信任务 + 按键任务）。

**产出**：能画出一张"系统里有哪些任务、谁阻塞谁、共享什么资源"的图——面试必问。

---

## 阶段 6：硬件进阶（与 5~7 并行，约 1 个月）

想往"嵌入式软硬件都能搞"发展：

- **立创EDA 画板**：从最小系统板（STM32 + 电源 + 晶振 + 下载口）画起，打样 5 元 10 片
- 焊接调试：贴片焊接、用万用表/逻辑分析仪排查问题
- 电源设计入门：LDO（AMS1117）vs DC-DC（MP1584）、滤波电容摆放
- 常见坑：晶振负载电容、去耦电容、地线处理

**产出**：自己画的一块能跑的板子（哪怕只是最小系统 + LED）。

---

## 阶段 7：Linux 嵌入式（选学/进阶，2~3 个月）

> 单片机岗和 Linux 岗是两条线，但市场更缺 Linux 嵌入式。先确认你真的要用 Linux：**学 Linux 的前提是阶段 4 已通关**。

### 7.1 Linux 基础（2~3 周）

- 装虚拟机（Ubuntu）或 WSL，熟悉命令、vim、shell 脚本
- 交叉编译概念：`arm-linux-gnueabihf-gcc` 和 `gcc` 的区别

### 7.2 Linux 应用编程（4~6 周）

- 文件 IO（open/read/write）、标准 IO
- 多进程：fork、exec、进程间通信（管道/共享内存/信号）
- 多线程：pthread、互斥锁、条件变量（面试高频）
- 网络编程：socket、TCP/UDP、select/poll/epoll

**项目**：写一个 TCP 服务器 + 客户端（比如远程控制小车/采集数据上报）。

> 买板子：**正点原子/野火 IMX6ULL 开发板**（约 ¥300~400，资料最全），或全志/瑞芯微的开发板。

### 7.3 嵌入式 Linux 系统（3~4 周）

- 系统组成：uboot（引导）→ 内核 → 根文件系统
- 会用 buildroot 或直接烧录厂家镜像；理解设备树（.dts）是什么
- 交叉编译一个 hello 程序放到板子上跑

### 7.4 驱动开发入门（4~6 周）

- 字符设备驱动框架：module_init/exit、file_operations、misc 设备
- LED 驱动实战（操作 GPIO）
- platform 总线 + 设备树匹配
- 中断、阻塞/非阻塞 IO（poll）

**产出**：在板子上跑通"自己写的 LED 驱动 + 应用层 ioctl 控制"。

---

## 阶段 8：综合项目 + 求职（2~3 个月）

### 项目建议（选 1 个深耕）

- **物联网网关**：STM32 采集 + ESP8266/4G 上传 + 服务器存储（软硬结合，面试最好讲）
- **智能家居中控**：RTOS 多任务 + 触摸屏 + WiFi
- **边缘计算盒**：Linux 板 + 摄像头 + 简单图像处理（进阶）

### 求职准备

- 简历：项目用 STAR 法则写，突出"遇到了什么问题、怎么解决"
- 八股高频：中断处理流程、栈与堆、I2C/SPI/UART 区别、RTOS 调度、优先级翻转、大小端、volatile 作用、编译过程
- 笔试题：位操作、链表反转、内存对齐（去刷《程序员面试宝典》嵌入式部分）

### 求职方向

| 方向 | 要求 | 薪资参考（国内） |
|------|------|------|
| 单片机/嵌入式软件工程师 | 阶段 1~5 | 8k~15k |
| Linux 应用开发 | 阶段 7.1~7.2 | 12k~20k |
| Linux 驱动工程师 | 阶段 7.3~7.4 | 15k~25k |
| 嵌入式系统架构 | 全栈 + 3~5 年经验 | 20k+ |

---

## 常见坑与建议

1. **不要沉迷视频**：看一小时视频不如写十行代码。每集视频跟着敲一遍。
2. **不要囤开发板**：一块 STM32 学完所有外设，再按项目买模块。
3. **遇到问题先查手册**，再百度/问 AI，最后才问人——调试能力是核心竞争力。
4. **写博客/GitHub 记录**：每完成一个实验写一篇笔记，面试时直接甩链接。
5. **英语别丢**：数据手册、芯片文档、Linux 文档都是英文。
6. **Git 尽早用**：从第一个项目就开始用 Git 管理代码。
7. **示波器/逻辑分析仪晚点买**：先用开发板自带的 LED + 串口打印调试，学会"软件打印调试"后再上仪器。

---

## 时间线速查

| 时间段 | 内容 | 里程碑 |
|--------|------|--------|
| 第 1 周 | 环境搭建 + C 语言热身 | 板子点亮 LED |
| 第 2~5 周 | C 补强 + 电路基础 + 51 | 51 综合项目完成 |
| 第 2~6 月 | STM32 外设 + 综合项目 | 智能小车/监测站跑通 |
| 第 7 月 | FreeRTOS | 多任务版项目 |
| 第 8~9 月 | Linux 系统 + 应用编程（可选） | TCP 通信项目 |
| 第 10~12 月 | Linux 驱动（可选） | 自写驱动点亮 LED |
| 第 13~15 月 | 综合项目 + 求职 | 简历投出 |

> 以上是按"业余 3~4 小时/天"估算；全职学习可压缩 1/3。
