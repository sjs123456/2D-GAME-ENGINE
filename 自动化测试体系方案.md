# Agent + MCP + Skills 自动化测试体系方案

> 适用平台：http://123.56.21.178:8080（自动化测试平台，登录/注册已试点）
> 基础：Reasonix（agent 运行时）· Playwright Python 1.61 · 项目已有 2 个 skill + 版本化测试资产
> 版本：v1.0　日期：2026-08-07

---

## 0. 现状盘点（已具备）

| 资产 | 位置 | 状态 |
|------|------|------|
| web-automation skill（Playwright 执行，subagent 模式） | `.reasonix/skills/web-automation/` | ✅ 已用 3 轮注册 + 3 轮登录测试验证 |
| generate-test-report skill（自包含 HTML 报告） | `.reasonix/skills/generate-test-report/` | ✅ 已产出 5 份报告 |
| 测试资产复用机制（版本化脚本/JSON/截图） | `.reasonix/tmp/` | ✅ 复用率 100%（每轮仅改动态部分） |
| 参数化报告生成器 | `.reasonix/tmp/gen-report.py` | ✅ `python gen-report.py <module> <version>` |
| MCP 接入 | 无 | ⬜ 待建 |

**已验证效果：** 登录/注册两模块 3 轮回归 0 回归差异；发现并持续跟踪 4 个缺陷（登录 BUG-001、注册 OBS-001/002/003）。

---

## 1. 总体架构（三层）

```
┌─────────────────────────────────────────────────────┐
│  Agent 编排层（Reasonix）                            │
│  主 agent：理解需求 → 规划用例 → 派发 → 汇总 → 出报告 │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │ 测试执行 sub │  │ 报告生成 sub  │  │ 缺陷登记 sub│  │
│  └──────┬──────┘  └──────┬───────┘  └─────┬──────┘  │
├─────────┼────────────────┼────────────────┼─────────┤
│  Skills 层（知识/流程固化）                    │       │
│  web-automation  generate-test-report        │       │
│  test-case-designer  defect-tracker (新增)   │       │
├─────────┼────────────────┼────────────────┼─────────┤
│  MCP 层（能力接入：浏览器/资产/缺陷/CI）       │       │
│  playwright-mcp  test-assets-mcp             │       │
│  defect-mcp  ci-mcp                          │       │
└─────────┼────────────────┼────────────────┼─────────┘
          └────────┬───────┴───────┬────────┘
          被测系统 123.56.21.178  测试资产库 .reasonix/tmp/
```

**分工原则：** Skills 固化"怎么做"（流程/模板/坑），MCP 提供"能做什么"（浏览器协议化操作、资产查询、缺陷落库），Agent 负责"决定做什么"（规划、派发、判定、汇报）。

---

## 2. Skills 层（执行与知识）

### 2.1 已有（保留并强化）

| Skill | 模式 | 职责 | 强化点 |
|-------|------|------|--------|
| web-automation | subagent | 浏览器自动化执行 + 测试资产复用 | ① 独立复核用例（如 TC05b）结果必须并入主 JSON；② 增加模块注册表（模块→最新版本号）查询入口 |
| generate-test-report | inline | 结果 JSON + 截图 → 自包含 HTML 报告 | 已参数化（gen-report.py），报告与数据版本对齐 |

### 2.2 新增建议

| Skill | 模式 | 职责 | 关键内容 |
|-------|------|------|----------|
| test-case-designer | subagent | 根据页面/接口/需求文档自动设计用例 | 等价类/边界值/错误推测模板；输出标准 JSON（直接喂给 web-automation）；用例编号、优先级、前置条件字段 |
| defect-tracker | inline | 缺陷登记与跟踪 | 缺陷唯一 ID（BUG/OBS-xxx）、影响用例、复现步骤、严重度、状态机（新发现→复现→修复→回归通过）；对接 MCP 落库 |
| ci-bridge | inline | 触发/汇报 CI 回归 | 调 CI 流水线、拉取测试结果、对比基线 |

---

## 3. MCP 层（能力接入）

Reasonix 通过 `reasonix.toml` 的 `[[plugins]]` 或 `.mcp.json` 接入（stdio/http/sse 三种传输，见 docs/GUIDE.zh-CN.md::s017）。

### 3.1 浏览器操作：Playwright MCP（官方）

```toml
[[plugins]]
name = "playwright-mcp"
type = "stdio"
command = "npx"
args = ["@playwright/mcp@latest", "--browser", "chromium", "--headless"]
```

**价值：** 浏览器操作协议化（`browser_navigate` / `browser_click` / `browser_snapshot` / `browser_screenshot` 等），agent 可少写胶水代码；与现有 Playwright Python skill 互补——MCP 适合交互式探索/调试，skill 脚本适合批量回归（复用机制、版本化落盘）。

### 3.2 测试资产：test-assets-mcp（✅ 已实现，`.reasonix/mcp/test-assets-mcp.py`）

```toml
[[plugins]]
name = "test-assets-mcp"
command = "python"
args = ["C:/.../.reasonix/mcp/test-assets-mcp.py"]   # 已注册
```

提供工具（5 个，实测可用）：
- `assets_list(module)`：模块已有脚本/JSON/截图/报告版本清单 → 直接得到"下次版本号"（`next_version`）
- `assets_read_json(module, version)`：读取指定版本结果，供对比
- `assets_register(module, version, script, json_file, screenshots)`：登记新产出资产
- `report_archive(module, version)`：报告归档到 `reports/` 并重建索引页
- `report_index()`：列出全部已归档报告

数据：`.reasonix/assets-index.json`（登记表）＋ `reports/index.html`（报告索引页）。

> 实现说明：原方案中 report-mcp 与 test-assets-mcp 合并为同一 server——二者同属"测试产物管理"能力域，合并可减少进程与配置开销；若未来需要独立部署/权限隔离，再拆分。

### 3.3 缺陷管理：defect-mcp（对接 Jira / 禅道 / 本地 JSON）

```
type = http / stdio（按后端选）
工具：defect_create / defect_query / defect_update_status
```

**价值：** 测试发现的缺陷（现 4 个）不再散落在报告里，进入可查询、可跟踪状态。

### 3.4 测试报告服务：report-mcp（可选）

- `report_latest(module)` → 最新报告路径/URL
- `report_archive(module, version)` → 归档到 `reports/` 目录并生成索引 `reports/index.html`

---

## 4. Agent 编排层

### 4.1 测试执行编排（推荐流程）

```
用户：再测下 X 页面
  │
  ├─ ① 主 agent：解析任务 → 查资产（test-assets-mcp.assets_list）
  │
  ├─ ② test-case-designer sub：按变更内容增补用例（可选）
  │
  ├─ ③ web-automation sub：复用脚本→只改动态部分→执行→结果落盘 vN+1
  │     （隔离上下文，只回传摘要，避免污染主会话）
  │
  ├─ ④ 主 agent：读取 vN vs vN+1 对比（assets_read_json）→ 判定
  │
  ├─ ⑤ defect-tracker：更新缺陷状态（新缺陷登记/确认复现/标记修复）
  │
  └─ ⑥ generate-test-report：出报告（gen-report.py module N+1）
```

### 4.2 关键设计

| 项 | 设计 |
|----|------|
| 上下文隔离 | 执行/报告/缺陷均走 subagent，主 agent 只收摘要，长任务不阻塞会话 |
| 资产版本化 | 脚本/JSON/截图/报告统一版本号对账，历史不覆盖（现有机制） |
| 独立复核 | 补充验证用例（TC05b 类）必须并入主结果 JSON（本次踩坑教训） |
| 失败策略 | 单用例超时记 BLOCKED 继续；用例级 try/except；选择器失效回退重探测 |
| 可追溯 | 每条结果含 script/run_at 字段；缺陷含复现证据截图路径 |

---

## 5. 测试资产与数据管理

```
.reasonix/tmp/
├── test-{module}-cases[-vN].py      # 测试脚本（版本化）
├── {module}-form-info.json          # 页面结构探测（复用，避免重复探索）
├── {module}-test-results[-vN].json  # 结果数据（历史保留）
├── {module}-TC{NN}[-vN].png         # 步骤截图
└── gen-report.py                    # 参数化报告生成器
```

**增强建议：**
1. 增加 `assets/index.json` 资产登记表（模块、最新版本、用例数、PASS/FAIL、时间、脚本名），由 test-assets-mcp 维护；
2. 报告归档目录 `reports/` + 索引页，跨轮次趋势可查（PASS 率、缺陷数变化曲线）。

---

## 6. CI/CD 集成（阶段 3）

```yaml
# .github/workflows/regression.yml
on: [push, workflow_dispatch]
jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.14" }
      - run: pip install playwright && python -m playwright install chromium
      - run: python test-register-cases-v4.py --json register-ci.json   # 复用既有脚本
      - run: python gen-report.py register 5                            # 出报告
      - uses: actions/upload-artifact@v4
        with: { path: reports/ }
```

**关键点：** CI 复用与本地完全相同的版本化脚本与报告生成器 → 本地/CI 结果天然可对比（基线一致）。

---

## 7. 分阶段落地路线

| 阶段 | 内容 | 产出 | 工作量 |
|------|------|------|--------|
| **P0（已完成）** | web-automation + generate-test-report + 资产复用机制 | 登录/注册 3 轮回归资产 + 5 份报告 | — |
| **P1（✅ 已完成 2026-08-08）** | 接入 playwright-mcp；新增 test-case-designer、defect-tracker skill；缺陷登记入库（`defects.json`，现有 4 缺陷） | MCP 可用（24 工具）、用例自动设计、缺陷可跟踪 | — |
| **P2（✅ 已完成 2026-08-08）** | 自研 test-assets-mcp（`assets-index.json` 登记表 + `reports/` 归档与索引；报告工具并入同一 server，见 §3.2） | 资产查询程序化（5 工具）、报告归档+索引 | — |
| **P3（✅ 已完成 2026-08-08）** | CI/CD 流水线：`.github/workflows/regression.yml`（复用版本化脚本 + gen-report.py，本地/CI 结果可对比）+ 本地驱动 `scripts/run-regression.py`；脚本路径可移植化（`TEST_WS` 环境变量） | 每次提交自动回归、失败自动出报告 | — |

---

## 8. 风险与注意事项

| 风险 | 缓解 |
|------|------|
| playwright-mcp 与现有 Python skill 职责重叠 | 定位互补：MCP 探索调试、skill 批量回归；同一测试用 skill 脚本保证复用与版本化 |
| 测试账号污染（已 4 个） | test-case-designer 统一账号命名规则（testuser_v{轮次}_{ts}），定期清理脚本；MCP 提供清理清单 |
| 被测系统改版导致选择器失效 | form-info.json 版本化，改版时重新探测并 bump；CI 失败自动提示结构变化 |
| 缺陷持续未修复（现 4 个） | defect-tracker 状态机 + 每轮回归强制核对缺陷复现情况，报告固化"缺陷复测确认"章节 |
| MCP 自研脚本可靠性 | 先 stdio + 本地 JSON 落库，验证稳定后再对接 HTTP/外部系统 |

---

## 附：现有缺陷清单（截至 2026-08-07，均已多轮确认未修复；已入库 `.reasonix/tmp/defects.json`，由 defect-tracker skill 维护）

| ID | 严重度 | 描述 | 影响用例 |
|----|--------|------|----------|
| BUG-001 | 中 | 登录失败（401）前端无任何错误提示 | 登录 TC02/TC03 |
| OBS-001 | 中 | 注册前端缺格式本地校验，后端英文错误透传 | 注册 TC03/TC04/TC06 |
| OBS-002 | 低 | 邮箱格式校验为 HTML5 英文气泡，风格不一致 | 注册 TC07 |
| OBS-003 | 低 | 确认密码为空提示「两次输入的密码不一致」文案偏差 | 注册 TC11 |
